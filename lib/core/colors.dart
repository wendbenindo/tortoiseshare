import 'package:flutter/material.dart';

// Palette de couleurs TortoiseShare 🐢
class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF4CAF50);      // Vert tortue
  static const Color secondary = Color(0xFF8BC34A);    // Vert clair
  static const Color accent = Color(0xFF795548);       // Marron carapace
  
  // Backgrounds
  static const Color background = Color(0xFFF5F5F5);
  static const Color backgroundLight = Color(0xFFFAFAFA);
  static const Color card = Colors.white;
  
  // Texte
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF666666);
  
  // Status
  static const Color success = Colors.green;
  static const Color error = Colors.red;
  static const Color warning = Colors.orange;
  static const Color info = Colors.blue;
}
